name: vartalap_store
description: SQLite-backed local store for Vartalap v3 (projections + outbound_ops).
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  sqflite_common: ^2.5.0
  sqflite_common_ffi: ^2.3.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
