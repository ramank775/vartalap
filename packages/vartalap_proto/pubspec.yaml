name: vartalap_proto
description: Generated protobuf bindings for Vartalap v3 sync wire schema.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  protobuf: ^6.0.0
  fixnum: ^1.1.0

dev_dependencies:
  lints: ^5.0.0
