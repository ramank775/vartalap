name: vartalap_sync
description: Outbound-op scheduler and sync layer for Vartalap v3.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  vartalap_store:
    path: ../vartalap_store
  vartalap_transport:
    path: ../vartalap_transport
  vartalap_proto:
    path: ../vartalap_proto

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
